<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper
  PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
  "https://mybatis.org/dtd/mybatis-3-mapper.dtd">

<!-- 

    java : mapper            mybatis : mapper
   ―――――――――――――――――――――――――――――――――――――――――――
   
      interface   <――――――――        XML
      method       ――――――――>       id
                           
                           namespace : Interface

 --> 
  
  
<!-- 호출하는 인터페이스 경로 작성 -->
<mapper namespace="com.gdu.myapp.mapper.UploadMapper">
 
 <resultMap type="UploadDto" id="UploadMap">
   <id     property="uploadNo"    column="UPLOAD_NO" /> <!-- jdbcType 은 생략 가능함 -->
   <result property="title"       column="TITLE"/> 
   <result property="contents"    column="CONTENTS"/> 
   <result property="createDt"    column="CREATE_DT"/> 
   <result property="modifyDt"    column="MODIFY_DT"/>
   <result property="attachCount" column="ATTACH_COUNT"/>
   <association property="user"  javaType="UserDto">     <!-- association : 1:M 관계 만들 때 사용 -->
      <id     property="userNo" column="USER_NO"/>
      <result property="email"  column="EMAIL"/>
   </association> 
 </resultMap>
 
 
 <!-- INSERT 수행 이전에 파라미터 UploadDto의 uploadNo속성(필드)에 UPLOAD_SEQ.NEXTVAL 값을 저장한다.  -->
 <insert id="insertUpload"
         parameterType="UploadDto">
   <!-- selectKey 를 이용해 UploadDto 에 upload -->
   <selectKey order="BEFORE" keyProperty="uploadNo" resultType="int">  
     SELECT UPLOAD_SEQ.NEXTVAL
       FROM DUAL
   </selectKey>   
    INSERT INTO UPLOAD_T (
        UPLOAD_NO
      , TITLE
      , CONTENTS
      , CREATE_DT
      , MODIFY_DT
      , USER_NO
    ) VALUES (
        #{uploadNo}
      , #{title}
      , #{contents}
      , TO_CHAR(CURRENT_DATE, 'YYYY-MM-DD HH24:MI:SS') <!-- 날짜 처리하는 방법 3가지 : CURRENT_DATE(Date 타입) , TIMESTAMP(Date 타입), TO_CAHR(String 타입 , 날짜를 문자열로 만들어주는 함수) -->
      , TO_CHAR(CURRENT_DATE, 'YYYY-MM-DD HH24:MI:SS')
      , #{user.userNo}
    )
        
 </insert>
  
  <insert id="insertAttach"
          parameterType="AttachDto">
    INSERT INTO ATTACH_T (
        ATTACH_NO
      , UPLOAD_PATH
      , FILESYSTEM_NAME
      , ORIGINAL_FILENAME
      , DOWNLOAD_COUNT
      , HAS_THUMBNAIL
      , UPLOAD_NO
    ) VALUES (
        ATTACH_SEQ.NEXTVAL
      , #{uploadPath}
      , #{filesystemName}
      , #{originalFilename}
      , 0
      , #{hasThumbnail}
      , #{uploadNo}      <!-- <selectKey> 로 인하여 파일 업로드 정보가 저장될 때, uploadNo를 service에서 알 수 있음  -->
    )
   
 </insert>
 
  <select id="getUploadCount">
   SELECT COUNT(*)
     FROM UPLOAD_T
 </select>
 
 <!-- UPLOAD_T 목록 -->
 <!-- ATTACH_COUNT -->
 <select id="getUploadList"
         parameterType="Map"
         resultMap="UploadMap">
  SELECT UPLOAD_NO, TITLE, CONTENTS, CREATE_DT, MODIFY_DT
         , USER_NO, EMAIL
         , ATTACH_COUNT
      FROM (SELECT ROW_NUMBER() OVER (ORDER BY UPLOAD_NO ${sort}) AS RN
                 , UP.UPLOAD_NO, UP.TITLE, UP.CONTENTS, UP.CREATE_DT, UP.MODIFY_DT
                 , USR.USER_NO, USR.EMAIL
                 , (SELECT COUNT(*)
                      FROM ATTACH_T ATC
                     WHERE UP.UPLOAD_NO = ATC.UPLOAD_NO) AS ATTACH_COUNT
              FROM USER_T USR INNER JOIN UPLOAD_T UP
                ON USR.USER_NO = UP.USER_NO)
     WHERE RN BETWEEN #{begin} AND #{end}
        
 </select>
 
 <!-- 게시글 상세보기 -->
 <select id="getUploadByNo"
         resultMap="UploadMap">
  SELECT UP.UPLOAD_NO, UP.TITLE, UP.CONTENTS, UP.CREATE_DT, UP.MODIFY_DT
       , USR.USER_NO, USR.EMAIL
    FROM USER_T USR INNER JOIN UPLOAD_T UP
      ON USR.USER_NO = UP.USER_NO
   WHERE UP.UPLOAD_NO = #{uploadNo}
 </select>
 
 </mapper>